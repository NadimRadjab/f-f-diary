class List {
  id: string;
  date: Date;
  totalcal: number;
  constructor(id: string, date: Date, totalcal: number) {
    this.id = id;
    this.date = date;
    this.totalcal = totalcal;
  }
}
export default List;
